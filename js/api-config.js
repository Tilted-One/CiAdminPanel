/**
 * API Configuration
 * Centralized base URL for all API endpoints
 */

const API = 'https://ciapi.jeramotors.ge';

// Export to global window object so all scripts can access it
window.API = API;
